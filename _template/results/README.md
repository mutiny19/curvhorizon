# Results & replication log

Plots, photos, and the narrative interpretation of each run live here. The raw
runs themselves live under [`../data/`](../data/), one subfolder per run named
`YYYY-MM-DD_contributor/`.

## Replication log

The `status` in `metadata.yml` must be supported by the rows below, not by
anyone's expectations. A refuted or inconclusive run is a real result — log it.

| Date | Contributor | Conditions | Outcome | Data | Notes |
|---|---|---|---|---|---|
| | | | replicated / refuted / inconclusive | `data/…` | |
